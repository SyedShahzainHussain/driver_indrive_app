class DistanceInfoModel {
  int? distance_value, duration_value;
  String? distance_text, duration_text,e_points;

  DistanceInfoModel({
    this.distance_value,
    this.duration_value,
    this.distance_text,
    this.duration_text,
    this.e_points
  });
  
}